
import 'package:flutter/material.dart';

class MyDeviceInfoWidget extends StatelessWidget {
  const MyDeviceInfoWidget({super.key});

  

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [

      ],
    );
  }
}